<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Items que se mostrarán en el Drawer -->
    <group android:id="@+id/nav_group" android:checkableBehavior="single">
        <!-- TOP LEVEL (los que quieras tener como principales) -->
        <item
            android:id="@+id/nav_profile"
            android:title="Mis Datos"
            android:icon="@drawable/user_ic" />

        <item
            android:id="@+id/nav_main_list"
            android:title="Lista Personal"
            android:icon="@drawable/burguer_menu_ic" />

        <item
            android:id="@+id/nav_second_list"
            android:title="Lista Genérica"
            android:icon="@drawable/ic_list" />
    </group>

    <!-- Logout (al final del Drawer) -->
    <item
        android:id="@+id/nav_logout"
        android:title="Logout"
        android:icon="@drawable/logout_ic"
        android:layout_gravity="bottom" />
</menu>
